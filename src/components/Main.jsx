import React, { StrictMode } from "react";
import ReactDOM from "react-dom";
import App from "../App";

ReactDOM.render(
  document.getElementById("root"),
  <StrictMode>
    <App />
  </StrictMode>
);
